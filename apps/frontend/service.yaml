apiVersion: v1
kind: Service

metadata:
  name: frontend-service
  namespace: frontend
  annotations:
    argocd.argoproj.io/sync-wave: "3"

spec:
  selector:
    app: frontend

  ports:
  - port: 80
    targetPort: 80

  type: NodePort